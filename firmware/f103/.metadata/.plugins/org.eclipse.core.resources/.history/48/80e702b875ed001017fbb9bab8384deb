//---------------------------- Ganpati Bappa Morya -----------------------

#ifndef __USBD_MIDI_IF_H
#define __USBD_MIDI_IF_H

#ifdef __cplusplus
extern "C" {
#endif

/* Includes ------------------------------------------------------------------*/
#include <stdint.h>
#include "usbd_midi.h"

/* Exported types ------------------------------------------------------------*/

/**
 * @brief  USB MIDI Interface callbacks
 *
 * Implemented in usbd_midi_if.c
 */
extern USBD_MIDI_ItfTypeDef USBD_MIDI_fops;

/* Exported functions --------------------------------------------------------*/

/**
 * @brief  Send MIDI packet to USB host
 * @param  data   Pointer to MIDI event packet (4 bytes per event)
 * @param  len    Length in bytes (must be multiple of 4)
 * @retval USBD status
 */
uint8_t USB_MIDI_Send(uint8_t *data, uint16_t len);

//void USB_MIDI_NoteOn(uint8_t channel, uint8_t note, uint8_t velocity);
//void USB_MIDI_NoteOff(uint8_t channel, uint8_t note, uint8_t velocity);
//void USB_MIDI_CC(uint8_t channel, uint8_t cc, uint8_t value);



#ifdef __cplusplus
}
#endif

#endif /* __USBD_MIDI_IF_H */
